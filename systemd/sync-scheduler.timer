[Unit]
Description=rclone-sync per-pair scheduler tick (jede Minute)

[Timer]
OnCalendar=*-*-* *:*:00
Persistent=false
AccuracySec=10s
RandomizedDelaySec=0
Unit=sync-scheduler.service

[Install]
WantedBy=timers.target
